@"
name: Python CI

on:
  push:
    branches: [ main, task-1 ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      - run: pip install -r requirements.txt
"@ | Out-File -FilePath .github\workflows\unittests.yml -Encoding UTF8